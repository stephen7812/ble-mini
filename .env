# 只有 VUE_APP_ 前缀的变量会被 @vue/cli-service 注入到代码里。
# 写成 VITE_ 之类的前缀不会报错，只会静默变成 undefined，然后悄悄走兜底值。
VUE_APP_API_BASE_URL=http://192.168.0.195:8090

# 切真机预览 / 真机调试前必须换成局域网 IP（换成你本机的实际 IP）：
# jsapi.site 是本机 hosts 里的名字，带上真机会走公网 DNS 而解析失败。
# VUE_APP_API_BASE_URL=http://192.168.31.13:8090
#
# 不要写 0.0.0.0 —— 那是服务端「监听所有网卡」的写法，作为客户端的目标地址
# 它指向的是「本机的未指定地址」，在手机上必然连不通，比 127.0.0.1 还差
# （127.0.0.1 至少在模拟器里是通的）。
